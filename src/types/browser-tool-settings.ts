/** Navegador controlado pelo assistente (Peça 19, Lote 5). */
export interface BrowserToolSettings {
  readonly enabled: boolean;
}

export const DEFAULT_BROWSER_TOOL_SETTINGS: BrowserToolSettings = {
  enabled: false,
};
